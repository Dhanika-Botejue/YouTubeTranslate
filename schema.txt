The following code is the schema inside my app.db file, which I am not commiting:

CREATE TABLE user(id INTEGER PRIMARY KEY AUTOINCREMENT, username TEXT NOT NULL UNIQUE, password TEXT NOT NULL);
CREATE TABLE sqlite_sequence(name,seq);
CREATE TABLE video (
id INTEGER PRIMARY KEY AUTOINCREMENT,
user_id INTEGER,
video_dst TEXT NOT NULL,
FOREIGN KEY (user_id) REFERENCES user(id) ON DELETE CASCADE
);